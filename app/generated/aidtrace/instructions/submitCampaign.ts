/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { AIDTRACE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const SUBMIT_CAMPAIGN_DISCRIMINATOR = new Uint8Array([
  33, 119, 203, 167, 224, 74, 32, 88,
]);

export function getSubmitCampaignDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SUBMIT_CAMPAIGN_DISCRIMINATOR,
  );
}

export type SubmitCampaignInstruction<
  TProgram extends string = typeof AIDTRACE_PROGRAM_ADDRESS,
  TAccountOrganization extends string | AccountMeta<string> = string,
  TAccountCampaign extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOrganization extends string
        ? ReadonlyAccount<TAccountOrganization>
        : TAccountOrganization,
      TAccountCampaign extends string
        ? WritableAccount<TAccountCampaign>
        : TAccountCampaign,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type SubmitCampaignInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type SubmitCampaignInstructionDataArgs = {};

export function getSubmitCampaignInstructionDataEncoder(): FixedSizeEncoder<SubmitCampaignInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: SUBMIT_CAMPAIGN_DISCRIMINATOR }),
  );
}

export function getSubmitCampaignInstructionDataDecoder(): FixedSizeDecoder<SubmitCampaignInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getSubmitCampaignInstructionDataCodec(): FixedSizeCodec<
  SubmitCampaignInstructionDataArgs,
  SubmitCampaignInstructionData
> {
  return combineCodec(
    getSubmitCampaignInstructionDataEncoder(),
    getSubmitCampaignInstructionDataDecoder(),
  );
}

export type SubmitCampaignInput<
  TAccountOrganization extends string = string,
  TAccountCampaign extends string = string,
  TAccountAuthority extends string = string,
> = {
  organization: Address<TAccountOrganization>;
  campaign: Address<TAccountCampaign>;
  authority: TransactionSigner<TAccountAuthority>;
};

export function getSubmitCampaignInstruction<
  TAccountOrganization extends string,
  TAccountCampaign extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof AIDTRACE_PROGRAM_ADDRESS,
>(
  input: SubmitCampaignInput<
    TAccountOrganization,
    TAccountCampaign,
    TAccountAuthority
  >,
  config?: { programAddress?: TProgramAddress },
): SubmitCampaignInstruction<
  TProgramAddress,
  TAccountOrganization,
  TAccountCampaign,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? AIDTRACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organization: { value: input.organization ?? null, isWritable: false },
    campaign: { value: input.campaign ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.organization),
      getAccountMeta(accounts.campaign),
      getAccountMeta(accounts.authority),
    ],
    data: getSubmitCampaignInstructionDataEncoder().encode({}),
    programAddress,
  } as SubmitCampaignInstruction<
    TProgramAddress,
    TAccountOrganization,
    TAccountCampaign,
    TAccountAuthority
  >);
}

export type ParsedSubmitCampaignInstruction<
  TProgram extends string = typeof AIDTRACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    organization: TAccountMetas[0];
    campaign: TAccountMetas[1];
    authority: TAccountMetas[2];
  };
  data: SubmitCampaignInstructionData;
};

export function parseSubmitCampaignInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSubmitCampaignInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      organization: getNextAccount(),
      campaign: getNextAccount(),
      authority: getNextAccount(),
    },
    data: getSubmitCampaignInstructionDataDecoder().decode(instruction.data),
  };
}
