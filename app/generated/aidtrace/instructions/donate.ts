/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findConfigPda, findDonationPda, findVaultPda } from "../pdas";
import { AIDTRACE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const DONATE_DISCRIMINATOR = new Uint8Array([
  121, 186, 218, 211, 73, 70, 196, 180,
]);

export function getDonateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DONATE_DISCRIMINATOR);
}

export type DonateInstruction<
  TProgram extends string = typeof AIDTRACE_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountOrganization extends string | AccountMeta<string> = string,
  TAccountCampaign extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountDonation extends string | AccountMeta<string> = string,
  TAccountDonor extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountOrganization extends string
        ? ReadonlyAccount<TAccountOrganization>
        : TAccountOrganization,
      TAccountCampaign extends string
        ? WritableAccount<TAccountCampaign>
        : TAccountCampaign,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountDonation extends string
        ? WritableAccount<TAccountDonation>
        : TAccountDonation,
      TAccountDonor extends string
        ? WritableSignerAccount<TAccountDonor> &
            AccountSignerMeta<TAccountDonor>
        : TAccountDonor,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DonateInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
  donationId: bigint;
};

export type DonateInstructionDataArgs = {
  amount: number | bigint;
  donationId: number | bigint;
};

export function getDonateInstructionDataEncoder(): FixedSizeEncoder<DonateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
      ["donationId", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DONATE_DISCRIMINATOR }),
  );
}

export function getDonateInstructionDataDecoder(): FixedSizeDecoder<DonateInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
    ["donationId", getU64Decoder()],
  ]);
}

export function getDonateInstructionDataCodec(): FixedSizeCodec<
  DonateInstructionDataArgs,
  DonateInstructionData
> {
  return combineCodec(
    getDonateInstructionDataEncoder(),
    getDonateInstructionDataDecoder(),
  );
}

export type DonateAsyncInput<
  TAccountConfig extends string = string,
  TAccountOrganization extends string = string,
  TAccountCampaign extends string = string,
  TAccountVault extends string = string,
  TAccountDonation extends string = string,
  TAccountDonor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config?: Address<TAccountConfig>;
  organization: Address<TAccountOrganization>;
  campaign: Address<TAccountCampaign>;
  vault?: Address<TAccountVault>;
  donation?: Address<TAccountDonation>;
  donor: TransactionSigner<TAccountDonor>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: DonateInstructionDataArgs["amount"];
  donationId: DonateInstructionDataArgs["donationId"];
};

export async function getDonateInstructionAsync<
  TAccountConfig extends string,
  TAccountOrganization extends string,
  TAccountCampaign extends string,
  TAccountVault extends string,
  TAccountDonation extends string,
  TAccountDonor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AIDTRACE_PROGRAM_ADDRESS,
>(
  input: DonateAsyncInput<
    TAccountConfig,
    TAccountOrganization,
    TAccountCampaign,
    TAccountVault,
    TAccountDonation,
    TAccountDonor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DonateInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountOrganization,
    TAccountCampaign,
    TAccountVault,
    TAccountDonation,
    TAccountDonor,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AIDTRACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    organization: { value: input.organization ?? null, isWritable: false },
    campaign: { value: input.campaign ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    donation: { value: input.donation ?? null, isWritable: true },
    donor: { value: input.donor ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.vault.value) {
    accounts.vault.value = await findVaultPda({
      campaign: expectAddress(accounts.campaign.value),
    });
  }
  if (!accounts.donation.value) {
    accounts.donation.value = await findDonationPda({
      campaign: expectAddress(accounts.campaign.value),
      donationId: expectSome(args.donationId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.organization),
      getAccountMeta(accounts.campaign),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.donation),
      getAccountMeta(accounts.donor),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getDonateInstructionDataEncoder().encode(
      args as DonateInstructionDataArgs,
    ),
    programAddress,
  } as DonateInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountOrganization,
    TAccountCampaign,
    TAccountVault,
    TAccountDonation,
    TAccountDonor,
    TAccountSystemProgram
  >);
}

export type DonateInput<
  TAccountConfig extends string = string,
  TAccountOrganization extends string = string,
  TAccountCampaign extends string = string,
  TAccountVault extends string = string,
  TAccountDonation extends string = string,
  TAccountDonor extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  organization: Address<TAccountOrganization>;
  campaign: Address<TAccountCampaign>;
  vault: Address<TAccountVault>;
  donation: Address<TAccountDonation>;
  donor: TransactionSigner<TAccountDonor>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: DonateInstructionDataArgs["amount"];
  donationId: DonateInstructionDataArgs["donationId"];
};

export function getDonateInstruction<
  TAccountConfig extends string,
  TAccountOrganization extends string,
  TAccountCampaign extends string,
  TAccountVault extends string,
  TAccountDonation extends string,
  TAccountDonor extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AIDTRACE_PROGRAM_ADDRESS,
>(
  input: DonateInput<
    TAccountConfig,
    TAccountOrganization,
    TAccountCampaign,
    TAccountVault,
    TAccountDonation,
    TAccountDonor,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DonateInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountOrganization,
  TAccountCampaign,
  TAccountVault,
  TAccountDonation,
  TAccountDonor,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AIDTRACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    organization: { value: input.organization ?? null, isWritable: false },
    campaign: { value: input.campaign ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    donation: { value: input.donation ?? null, isWritable: true },
    donor: { value: input.donor ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.organization),
      getAccountMeta(accounts.campaign),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.donation),
      getAccountMeta(accounts.donor),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getDonateInstructionDataEncoder().encode(
      args as DonateInstructionDataArgs,
    ),
    programAddress,
  } as DonateInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountOrganization,
    TAccountCampaign,
    TAccountVault,
    TAccountDonation,
    TAccountDonor,
    TAccountSystemProgram
  >);
}

export type ParsedDonateInstruction<
  TProgram extends string = typeof AIDTRACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    organization: TAccountMetas[1];
    campaign: TAccountMetas[2];
    vault: TAccountMetas[3];
    donation: TAccountMetas[4];
    donor: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: DonateInstructionData;
};

export function parseDonateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDonateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      organization: getNextAccount(),
      campaign: getNextAccount(),
      vault: getNextAccount(),
      donation: getNextAccount(),
      donor: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getDonateInstructionDataDecoder().decode(instruction.data),
  };
}
