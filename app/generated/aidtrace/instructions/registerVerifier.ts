/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findVerifierRecordPda } from "../pdas";
import { AIDTRACE_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const REGISTER_VERIFIER_DISCRIMINATOR = new Uint8Array([
  67, 234, 172, 169, 184, 188, 145, 156,
]);

export function getRegisterVerifierDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_VERIFIER_DISCRIMINATOR,
  );
}

export type RegisterVerifierInstruction<
  TProgram extends string = typeof AIDTRACE_PROGRAM_ADDRESS,
  TAccountOrganization extends string | AccountMeta<string> = string,
  TAccountVerifierRecord extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOrganization extends string
        ? ReadonlyAccount<TAccountOrganization>
        : TAccountOrganization,
      TAccountVerifierRecord extends string
        ? WritableAccount<TAccountVerifierRecord>
        : TAccountVerifierRecord,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterVerifierInstructionData = {
  discriminator: ReadonlyUint8Array;
  verifier: Address;
};

export type RegisterVerifierInstructionDataArgs = { verifier: Address };

export function getRegisterVerifierInstructionDataEncoder(): FixedSizeEncoder<RegisterVerifierInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["verifier", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_VERIFIER_DISCRIMINATOR }),
  );
}

export function getRegisterVerifierInstructionDataDecoder(): FixedSizeDecoder<RegisterVerifierInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["verifier", getAddressDecoder()],
  ]);
}

export function getRegisterVerifierInstructionDataCodec(): FixedSizeCodec<
  RegisterVerifierInstructionDataArgs,
  RegisterVerifierInstructionData
> {
  return combineCodec(
    getRegisterVerifierInstructionDataEncoder(),
    getRegisterVerifierInstructionDataDecoder(),
  );
}

export type RegisterVerifierAsyncInput<
  TAccountOrganization extends string = string,
  TAccountVerifierRecord extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  organization: Address<TAccountOrganization>;
  verifierRecord?: Address<TAccountVerifierRecord>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  verifier: RegisterVerifierInstructionDataArgs["verifier"];
};

export async function getRegisterVerifierInstructionAsync<
  TAccountOrganization extends string,
  TAccountVerifierRecord extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AIDTRACE_PROGRAM_ADDRESS,
>(
  input: RegisterVerifierAsyncInput<
    TAccountOrganization,
    TAccountVerifierRecord,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterVerifierInstruction<
    TProgramAddress,
    TAccountOrganization,
    TAccountVerifierRecord,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AIDTRACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organization: { value: input.organization ?? null, isWritable: false },
    verifierRecord: { value: input.verifierRecord ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.verifierRecord.value) {
    accounts.verifierRecord.value = await findVerifierRecordPda({
      organization: expectAddress(accounts.organization.value),
      verifier: expectSome(args.verifier),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.organization),
      getAccountMeta(accounts.verifierRecord),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterVerifierInstructionDataEncoder().encode(
      args as RegisterVerifierInstructionDataArgs,
    ),
    programAddress,
  } as RegisterVerifierInstruction<
    TProgramAddress,
    TAccountOrganization,
    TAccountVerifierRecord,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type RegisterVerifierInput<
  TAccountOrganization extends string = string,
  TAccountVerifierRecord extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  organization: Address<TAccountOrganization>;
  verifierRecord: Address<TAccountVerifierRecord>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  verifier: RegisterVerifierInstructionDataArgs["verifier"];
};

export function getRegisterVerifierInstruction<
  TAccountOrganization extends string,
  TAccountVerifierRecord extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AIDTRACE_PROGRAM_ADDRESS,
>(
  input: RegisterVerifierInput<
    TAccountOrganization,
    TAccountVerifierRecord,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterVerifierInstruction<
  TProgramAddress,
  TAccountOrganization,
  TAccountVerifierRecord,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AIDTRACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    organization: { value: input.organization ?? null, isWritable: false },
    verifierRecord: { value: input.verifierRecord ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.organization),
      getAccountMeta(accounts.verifierRecord),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getRegisterVerifierInstructionDataEncoder().encode(
      args as RegisterVerifierInstructionDataArgs,
    ),
    programAddress,
  } as RegisterVerifierInstruction<
    TProgramAddress,
    TAccountOrganization,
    TAccountVerifierRecord,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterVerifierInstruction<
  TProgram extends string = typeof AIDTRACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    organization: TAccountMetas[0];
    verifierRecord: TAccountMetas[1];
    authority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: RegisterVerifierInstructionData;
};

export function parseRegisterVerifierInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterVerifierInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      organization: getNextAccount(),
      verifierRecord: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterVerifierInstructionDataDecoder().decode(instruction.data),
  };
}
