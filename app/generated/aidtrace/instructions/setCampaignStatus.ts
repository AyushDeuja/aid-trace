/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { findConfigPda } from "../pdas";
import { AIDTRACE_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";
import {
  getCampaignStatusDecoder,
  getCampaignStatusEncoder,
  type CampaignStatus,
  type CampaignStatusArgs,
} from "../types";

export const SET_CAMPAIGN_STATUS_DISCRIMINATOR = new Uint8Array([
  228, 16, 24, 238, 203, 46, 69, 189,
]);

export function getSetCampaignStatusDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_CAMPAIGN_STATUS_DISCRIMINATOR,
  );
}

export type SetCampaignStatusInstruction<
  TProgram extends string = typeof AIDTRACE_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountOrganization extends string | AccountMeta<string> = string,
  TAccountCampaign extends string | AccountMeta<string> = string,
  TAccountAdmin extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountOrganization extends string
        ? ReadonlyAccount<TAccountOrganization>
        : TAccountOrganization,
      TAccountCampaign extends string
        ? WritableAccount<TAccountCampaign>
        : TAccountCampaign,
      TAccountAdmin extends string
        ? ReadonlySignerAccount<TAccountAdmin> &
            AccountSignerMeta<TAccountAdmin>
        : TAccountAdmin,
      ...TRemainingAccounts,
    ]
  >;

export type SetCampaignStatusInstructionData = {
  discriminator: ReadonlyUint8Array;
  nextStatus: CampaignStatus;
};

export type SetCampaignStatusInstructionDataArgs = {
  nextStatus: CampaignStatusArgs;
};

export function getSetCampaignStatusInstructionDataEncoder(): FixedSizeEncoder<SetCampaignStatusInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["nextStatus", getCampaignStatusEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_CAMPAIGN_STATUS_DISCRIMINATOR }),
  );
}

export function getSetCampaignStatusInstructionDataDecoder(): FixedSizeDecoder<SetCampaignStatusInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["nextStatus", getCampaignStatusDecoder()],
  ]);
}

export function getSetCampaignStatusInstructionDataCodec(): FixedSizeCodec<
  SetCampaignStatusInstructionDataArgs,
  SetCampaignStatusInstructionData
> {
  return combineCodec(
    getSetCampaignStatusInstructionDataEncoder(),
    getSetCampaignStatusInstructionDataDecoder(),
  );
}

export type SetCampaignStatusAsyncInput<
  TAccountConfig extends string = string,
  TAccountOrganization extends string = string,
  TAccountCampaign extends string = string,
  TAccountAdmin extends string = string,
> = {
  config?: Address<TAccountConfig>;
  organization: Address<TAccountOrganization>;
  campaign: Address<TAccountCampaign>;
  admin: TransactionSigner<TAccountAdmin>;
  nextStatus: SetCampaignStatusInstructionDataArgs["nextStatus"];
};

export async function getSetCampaignStatusInstructionAsync<
  TAccountConfig extends string,
  TAccountOrganization extends string,
  TAccountCampaign extends string,
  TAccountAdmin extends string,
  TProgramAddress extends Address = typeof AIDTRACE_PROGRAM_ADDRESS,
>(
  input: SetCampaignStatusAsyncInput<
    TAccountConfig,
    TAccountOrganization,
    TAccountCampaign,
    TAccountAdmin
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetCampaignStatusInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountOrganization,
    TAccountCampaign,
    TAccountAdmin
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AIDTRACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    organization: { value: input.organization ?? null, isWritable: false },
    campaign: { value: input.campaign ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.organization),
      getAccountMeta(accounts.campaign),
      getAccountMeta(accounts.admin),
    ],
    data: getSetCampaignStatusInstructionDataEncoder().encode(
      args as SetCampaignStatusInstructionDataArgs,
    ),
    programAddress,
  } as SetCampaignStatusInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountOrganization,
    TAccountCampaign,
    TAccountAdmin
  >);
}

export type SetCampaignStatusInput<
  TAccountConfig extends string = string,
  TAccountOrganization extends string = string,
  TAccountCampaign extends string = string,
  TAccountAdmin extends string = string,
> = {
  config: Address<TAccountConfig>;
  organization: Address<TAccountOrganization>;
  campaign: Address<TAccountCampaign>;
  admin: TransactionSigner<TAccountAdmin>;
  nextStatus: SetCampaignStatusInstructionDataArgs["nextStatus"];
};

export function getSetCampaignStatusInstruction<
  TAccountConfig extends string,
  TAccountOrganization extends string,
  TAccountCampaign extends string,
  TAccountAdmin extends string,
  TProgramAddress extends Address = typeof AIDTRACE_PROGRAM_ADDRESS,
>(
  input: SetCampaignStatusInput<
    TAccountConfig,
    TAccountOrganization,
    TAccountCampaign,
    TAccountAdmin
  >,
  config?: { programAddress?: TProgramAddress },
): SetCampaignStatusInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountOrganization,
  TAccountCampaign,
  TAccountAdmin
> {
  // Program address.
  const programAddress = config?.programAddress ?? AIDTRACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    organization: { value: input.organization ?? null, isWritable: false },
    campaign: { value: input.campaign ?? null, isWritable: true },
    admin: { value: input.admin ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.organization),
      getAccountMeta(accounts.campaign),
      getAccountMeta(accounts.admin),
    ],
    data: getSetCampaignStatusInstructionDataEncoder().encode(
      args as SetCampaignStatusInstructionDataArgs,
    ),
    programAddress,
  } as SetCampaignStatusInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountOrganization,
    TAccountCampaign,
    TAccountAdmin
  >);
}

export type ParsedSetCampaignStatusInstruction<
  TProgram extends string = typeof AIDTRACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    organization: TAccountMetas[1];
    campaign: TAccountMetas[2];
    admin: TAccountMetas[3];
  };
  data: SetCampaignStatusInstructionData;
};

export function parseSetCampaignStatusInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetCampaignStatusInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      organization: getNextAccount(),
      campaign: getNextAccount(),
      admin: getNextAccount(),
    },
    data: getSetCampaignStatusInstructionDataDecoder().decode(instruction.data),
  };
}
